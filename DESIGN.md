# DESIGN — sutra-cover

> 版本 v1.2｜最後更新 2026-09-24

「怎麼用」見 [README](./README.zh-Hant.md)；本文記**為什麼長這樣**。家族共同規範只標引用、不重複。

## 1. 緣起與路徑

前身是 InProgress 的 `public/lib/cover.html`：一支 67 行的單檔頁，題名與作者寫死在 script 裡、
改內容要改原始碼，版式本身已經定稿。owner 2026-09-23 要求把它做成一支 app：
**輸入題名與譯者／作者即可照那個版式列印，並可指定題名的字級與字距。**

走 **Path A**（WORKFLOW）——原型是粗原型（inline style／script、無 i18n／主題），
**拿來讀懂行為、抄決策，不是拿來搬**。原型檔本身未動。

## 2. 拍板的決定（owner 2026-09-23）

| 問題 | 決定 | 理由 |
|---|---|---|
| 名稱 | `sutra-cover` | 直白；論、疏也適用（「sutra」在此泛指典籍） |
| 紙張 | **固定 A4 直式** | 原型沒有 `@page`，邊界交給印表機 ⇒ 預覽不可能精確 |
| 作者欄 | **單一自由文字欄** | 「撰／譯／述／造／集」由使用者自己打，全形空格照原樣 |
| 可見性 | **public** | 無 DB、不帶字型，沒有公開的障礙（v1.0 時是零後端；§10 之後有一支只寫自己資料夾的 route） |
| 可調範圍 | **只有題名的字級與字距** | 照需求；作者欄、上方留白、兩欄間距維持原型值 |
| 題名過長 | **警示、不自動縮小** | 字級與字距是使用者指定的，程式不該偷改 |
| 字型 | **偵測、講明，不偷換** | 見 §5 |
| 儲存（追加） | **一個編號一份**；編號**不印**在封面上；清單**每筆可刪**（移進 `.bak/`） | 見 §10 |

## 3. 版式：逐項照原型，只有一處改寫

| 項目 | 原型 | 本 app |
|---|---|---|
| 直排、題名在右、作者在左、底部對齊、置中 | ✓ | 相同 |
| 上方留白 | `body { padding-top:125px }` | `.cover { padding-top:125px }` |
| 題名 | `36pt`／`1.25em`／`margin-inline-end: −字距` | 可調；抵銷值與字距**同一個數字** |
| 作者 | `16pt`／`0.2em`／`padding-bottom:0.3em` | 相同（固定） |
| 兩欄間距 | `0.8em`（以 body 預設 16px 計＝12.8px） | `.cover` **明寫 `12pt`** |
| 紙張與邊界 | 無 `@page`，交給印表機 | **`@page A4 / margin 0` ＋ 紙內留邊 10mm** |

- **兩欄間距**：原型的 `.cover` 字級來自瀏覽器預設 16px，**不是題名字級**，所以間距不隨題名縮放。
  而 Materialize 會把 `html` 的字級改成 14–15px——照抄 `0.8em` 的話間距會**安靜地變小**，所以明寫 `12pt`。
- **唯一的改寫是紙張**：原型的實際上緣位置＝「印表機預設邊界 ＋ 125px」，換一台印表機就變。
  改成 `@page margin 0` 之後，紙內留邊由 `.sheet` 的 padding（10mm）提供，**畫面上那張紙就是印出來那張紙**。
- **驗收方式**：在 3001 上開原型、在本 app 開同一組參數，以不受 transform 影響的 `offset*` 量：
  題名高 **540**／作者高 **160**／底部差 **0**／兩欄間距 **13** px——**四項逐一相同**。

## 4. 預覽＝列印

- `.sheet` 以真實尺寸（`210mm × 297mm`）排版，畫面上用 `transform: scale()` 縮到欄寬；
  `transform` 不影響版面盒，所以外框 `.sheet-frame` 的高度由控制器補。
- 列印時 `@media print` 藏掉表單、側鍵、提示與 toast，把 `.sheet` 的 transform 拿掉。
  **不另組一份列印用 DOM**——兩份 DOM 就是兩份可以漂開的版式。
- 列印時 `.sheet` 高 `296.5mm`：整頁高 297mm 時 Chrome 偶爾多吐一張白紙；內容由上緣起排，少 0.5mm 只動到下留邊。
- dark 主題列印回淺色（§5.1 的 `color-scheme` 與 transition 兩個坑）。
- 以 headless Chrome `--print-to-pdf` 實測：**1 頁、595 × 842pt（A4）、預設 dark 仍是白紙**；
  12 字超長題名同樣 1 頁（超出部分被裁掉，畫面上有紅線與警示）。
- 列印期間把 `document.title` 換成「封面：題名」（同原型），存 PDF 時的預設檔名就是它；印完換回。

## 5. 字型：Weibei TC

- 那是 macOS 的系統字型（文鼎科技，© Arphic），**沒有再散布授權** ⇒ 只以族名引用、**不進 repo**
  （家族 memory `no-licence-means-no-redistribution`）。
- 別台電腦沒有它時，瀏覽器會**無聲地**退回一般明體——使用者印出來才發現不是魏碑。
  故開啟時**量像素**判斷有沒有真的用上，並分三態：
  - **用上**／**沒用上**：`'Weibei TC', serif` 與純 `serif` 畫同一串字，像素相同＝沒用上。
  - **量不出來**：對照組 `serif` 與 `sans-serif` 畫出來一樣時（canvas 被隱私保護遮掉等），
    **不落進任何一個結論**。
- 反向驗證過：不存在的族名畫出來與 `serif` 逐位元組相同；`Weibei TC` 不同。
- **`document.fonts.check()` 不能用**：對本機字型它一律回 `true`（實測對 Weibei 也是 true）。
- **不以寬度偵測**：CJK 字元的前進寬度恆為 1em，換哪支字型都一樣，寬度法必然回報「沒裝」。

## 6. 版面量測

- 「佔可用高度」＝題名欄實際佔高 ÷（紙高 − 上下留邊 − 上方留白），**量 DOM**（`offsetHeight`）。
  `getBoundingClientRect` 量到的是縮放後的值，**不可以用**。
- lib 的 `maxChars()` 是估計（假設每字前進 1em）：`n·size·(1+ls) − size·ls ≤ 可用高度`。
  原型值 36pt／1.25em 在 A4 上約 **9 字**。它與實測在純漢字題名時相同（127mm／127mm），
  含半形字母時可能不同——所以畫面上標「約」，而判斷超出與否一律看量測值。
- 三段：`ok`／`tight`（> 95%）／`over`（> 100%，紙的下留邊畫紅線）。

## 7. 參數與網址

- 參數全寫在網址列（`?c=&t=&a=&s=&ls=`），**複製連結仍是一種存檔**（同 `circle-text`）；
  只帶 `?c=T2428` 的連結＝開啟那一份已存的封面（§10）。
- 重寫網址時**保留不歸本 app 管的參數**（例如 i18n 的 `?lang=`）——第一版沒保留，
  帶 `?lang=ja` 開進來的頁面在第一次重繪時就把它丟了，重新整理後變回英文。
- 文字欄：去掉換行與控制字元（直排裡換行＝換欄）、截到 200 碼位，**不 trim**（`trim()` 會剝掉 U+3000）。
  `''` 是合法值，**不補回範例**。
- 數字：夾到範圍（字級 8–96pt、字距 0–3em）並對齊 step，非數字退回原型值。

## 8. 刻意沒做的

- **作者欄的字級／字距、上方留白、兩欄間距不開放調整**——需求沒要求；要開時 lib 的常數已集中在一處。
- **紙張選單**：owner 選固定 A4。要加時 `PAGE` 與 `@page` 要一起動（verify 第 14 條會抓不一致）。
- **自動縮小題名**：會改掉使用者指定的值，改為警示。
- **App icon 徽章**（§5.5 甲）：`#setting-menu` 用固定語彙的 `folder_open`（§5.6），icon 只用在 favicon／PWA。
- **同一個編號存多份**（例如多卷典籍各一張）：owner 選「一個編號一份」。要改的話鍵會變成「編號＋序號」，
  牽動檔名、清單與 `?c=` 深連結三處。
- **登入**：沒有。3001 對區網開放，連得到的人都能存、刪——與孵化器裡多數 app 相同；刪除只是移進 `.bak/`。

## 9. 契約檢查（`npm run verify`，36 條）

lib 行為（1–13，含編號規則、數字感知排序、清單篩選）／CSS 與 lib 常數一致（14）／抵銷規則的特異度（15）／
列印回淺色並藏掉清單（16）／i18n 三語 key、使用中的 key、參數集合、§6 正典文案（17–20）／
id 對應、側鍵順序（21–22）／清除鈕的位置、只清三欄、三欄間距（23–25）／Sidenav 綁 `onCloseStart`（26）／
後端不另寫驗證（27）／lib 不碰 DOM（28）／NUL 與實體 U+3000（29）／共用件 byte-identical（30）／
**API 在暫存資料夾裡實跑**（31–36）。

**反向驗證過 8 個案例**（`SUTRA_COVER_ROOT=<改壞的副本>`）：抵銷規則降回 `.cv-title`、不保留 `lang`、
改用 `trim()`、`maxChars` 多算一、ja `tool.mode` 漂掉、ja 少一個 key、上方留白與 lib 不一致、
lib 寫成實體 U+3000——**各自在預期的那一條變紅**。
§10 加入時另反向驗證 8 個：新建不用 `wx`、編號不轉大寫、綁 `onCloseEnd`、route 自己寫一份編號規則、
列印不藏側欄、資料夾不存在回 500、讀不進來的檔安靜略過、刪除直接 `unlink`——同樣各自變紅。

## 10. 儲存與封面清單（owner 2026-09-23 追加）

需求：側鍵加一顆「儲存」、資料多一欄**典籍編號**（即身成佛義是 `T2428`），並照 `markdown-reader`
的方式在右邊加一份封面清單。

- **資料層＝ DATABASE_GUIDELINES §0 的層 1**：一份封面一個 `public/upload/sutra-cover/<編號>.json`
  （`code`／`title`／`author`／`size`／`ls`／`createdAt`／`updatedAt`）。資料不跨 app 共享、數量是人手打的規模
  ⇒ 不必爬到 SQLite 或 PostgreSQL。清單就是列資料夾，與 `markdown-reader` 的檔案清單同一個模型。
- **一個編號一份，編號就是檔名**。⚠️ 因此**一律轉大寫**：macOS 的檔名不分大小寫，
  `t2428.json` 與 `T2428.json` 是同一個檔——不轉的話，畫面上兩個「不同的」鍵會互相覆寫。
  字元限定 ASCII 英數與 `. _ -`、開頭英數、不得含 `..`、最長 32（`T2428`、`X1000`、`T1796-01` 這類寫法都放得進去；沒有逐一核對過 CBETA 全部的編號格式）。
- **儲存先試新建，409 才問要不要覆寫**。「先查清單、看有沒有，再決定」是 TOCTOU（§3.3）——
  兩個分頁同時新建同一個編號，後寫的會安靜地蓋掉先寫的。後端新建用 `fs.open(…,'wx')`：
  實測 20 個併發請求只有 1 個成功、19 個 409。覆寫會先把舊檔備份到 `.bak/`，並保留 `createdAt`。
- **刪除＝移進 `.bak/`**（owner 選「每筆可刪」＋ §3.4 的精神）；`.bak/` 是 dotfile 資料夾，
  `express.static` 預設不送出（實測 404）。
- **後端的驗證載入前端的 lib**（`vm` 跑 `sutra-cover-lib.js`，取 `cleanCode`／`isValidCode`／`toRecord`）。
  分兩份寫的話，遲早會出現「畫面上存得進去、伺服器擋下來」或反過來；verify 第 27 條擋著。
- **讀不進來的檔不安靜略過**：壞掉的 JSON、檔名不合規（含大小寫不是正規形的）都列進 `skipped`、
  清單底下寫出來——少一筆與「從來沒存過」在畫面上一模一樣。
- **清單**：右側 Sidenav（`edge:'right'`），每列是編號（等寬）＋題名，右側一顆刪除鈕；
  篩選框掛共用件 `filter-clear`（§5.12），比對編號／題名／作者。依編號**數字感知**排序（T262 在 T2428 前）。
  ⚠️ `sidenav-open` 在 `onCloseStart` 拿掉——`onCloseEnd` 在背景分頁的動畫跑不完時永遠不來。
- **i18n**：`toast.saveFail`／`toast.deleteFail`／`toast.deleted`／`toast.listFail`／`toast.loadFail`
  逐字照家族多數與 §6 正典表。⚠️ `tool.menu`／`side.header` **刻意**寫「封面清單」／「典籍封面」
  而不是多數的「檔案清單」——列的是封面、使用者不必知道它們是檔案；理由登記在 `meta_i18n.fd_note`。
- ⚠️ 實測時側欄有一次打開後 600ms 內自己關了、`isOpen` 為 false，**之後三次重現不了**
  （含掛上 capture 記錄器的那次：沒有任何 `close()` 呼叫、也沒有額外點擊）。
  最可能是 preview pane 自己產生的點擊落在遮罩上（家族 memory `preview-pane-phantom-clicks`）；
  **要以真滑鼠確認**。

## 11. 清除鈕與欄位間距（owner 2026-09-24 追加）

- **內容卡的「列印」前面一顆清除鈕**（圖示 `clear`——§5.6「只清畫面、不刪檔」的那一個，不是 `delete_sweep`）。
  **只清編號／題名／作者三欄**：字級與字距是版式設定，清掉內容時通常還要沿用；也不碰伺服器上的任何檔案。
  清完脫離清單上「目前這一份」、游標回到編號欄。不問確認——它沒有動到任何存下來的東西，而重填比按取消還快。
  反向驗證過：清除也清了字級、清除鈕移到列印後面、題名欄少了 `.text-field`——各自變紅。
- **兩者都空時換一句提示**：只有題名空時是「只會印出譯者／作者」；兩者都空時那句話就錯了，改說「印出來會是一張白紙」。
- **三欄與前一個元素的間距**：先量才改（家族 memory `is-it-really-the-margin`）。Materialize 的 label 浮起時
  `translateY(-14px)`，正好吃掉 14px 的預設 `margin-top` ⇒ label 上緣與前一個元素的下緣**間距 0px**（三欄皆然）。
  `margin-top` 改 28px 之後實量三欄皆 **14px**。
  ⚠️ 截圖時 label 看起來疊在值上面——那是 0.2 秒浮起動畫的中途（`label.active` 已在、`transform` 已是 `-14px`），
  重截即正常；**不要為此去改 label 的定位**。
